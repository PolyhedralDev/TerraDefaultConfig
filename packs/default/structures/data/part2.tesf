id "part2";

num iter = 0;

while(iter < 5) {
    iter = iter + 1;
    block(iter, 0, 0, "minecraft:redstone_block");
}

iter = 0;

while(iter > -5) {
    iter = iter - 1;
    block(iter, 0, 0, "minecraft:redstone_block");
}